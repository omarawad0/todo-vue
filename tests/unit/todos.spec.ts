/* import { mount } from "@vue/test-utils";
import Todos from "@/components/Todos.vue";
import { LIMIT } from "@/core/apis/todoApis";

describe("Todos.vue", () => {
  it("renders the fetched numbers of todos on first load", () => {
    const wrapper = mount(Todos);
    const todos = wrapper.findAll('data-test="todo"');
    expect(todos).toHaveLength(LIMIT);
  });
});
 */
